{% extends 'base.html.twig' %}

{% block title "Recette : " ~ recipe.title %}

{% block body %}
    <h1 class="text-uppercase">
        {{ recipe.title }}
    </h1>
    <p>
        {{ recipe.content | nl2br }}
    </p>
{% endblock %}
